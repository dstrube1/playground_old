To run type in:
bin/fizzbuzz


Code is in the lib folder.

Tests are in the spec folder.

To run the tests make sure you have the rspec gem installed and then type in:
rspec spec --format doc --color